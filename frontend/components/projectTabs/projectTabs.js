'use strict';

angular.module('mifortTimesheet')
    .directive('projectTabs', function($location, preferences) {
        return {
            scope: true,
            link: function (scope, element, attrs) {

            }
        }
    });
